# Dashboard Import Addon

dashboard_import:
  package_import_url: github://azoninc/doorman/firmware/examples/${host_platform}.${api_variant}.${firmware_type}.${branch}.example.yaml@${branch}
  import_full_config: true