# Dashboard Import Addon

dashboard_import:
  package_import_url: github://azoninc/doorman/firmware/examples/${host_platform}.${firmware_type}.${firmware_type}.example.${branch}.yaml@${branch}
  import_full_config: true