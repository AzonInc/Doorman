import { css } from "lit";

export default css`
  :host, button, select, input {
    font-family: var(--vp-font-family-base);
    --primary-color: #9269fe;
    transition: all 350ms !important;
  }
`;
